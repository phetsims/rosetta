{
  "name": "rosetta",
  "private": true,
  "type": "module",
  "workspaces": [
    "src/client",
    "src/server",
    "scripts"
  ],
  "scripts": {
    "lint": "grunt --gruntfile Gruntfile.cjs lint",
    "start": "npm run lint && npm run build --workspace=client && node src/server/app.js"
  }
}
