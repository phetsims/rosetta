{
  "name": "rosetta",
  "private": true,
  "type": "module",
  "workspaces": [
    "src/client",
    "src/server"
  ],
  "scripts": {
    "lint": "grunt --gruntfile Gruntfile.cjs lint",
    "dev": "grunt --gruntfile Gruntfile.cjs lint && webpack --mode=development && node src/server/app.js",
    "production": "grunt --gruntfile Gruntfile.cjs lint && webpack --mode=production && node src/server/app.js"
  }
}
